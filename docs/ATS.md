# ATS Engine

- Central configuration in `src/config/ats.js`.
- Deterministic and local analysis via `ATSService` and analyzers extending `BaseATSAnalyzer`.
- Rule-based heuristics for readability and grammar. AI is strictly opt-in ("Improve with AI").
- Caches results locally.
