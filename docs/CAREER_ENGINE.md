# Career Engine

- Unified entry point via `CareerCoachService`.
- Sub-engines (`CareerRoadmapEngine`, `SalaryInsightEngine`) provide specific insights.
- The "Career Readiness Score" is a 0-100 metric calculated dynamically.
